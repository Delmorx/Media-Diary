{
  "name": "media-diary",
  "version": "1.0.0",
  "description": "A personal media review and tracking platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd client && npm run build && cd ..",
    "install-all": "npm install && cd client && npm install && cd ..",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "keywords": ["media", "reviews", "diary"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.x"
  }
}
